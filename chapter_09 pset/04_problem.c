#include <stdio.h>
typedef struct complex{
    int real;
    int imag;

} complex;
int main(){
    complex c={1,2};
    printf(" the value of vector is %di + %dj", v.real, v.imag);
    return 0;
}